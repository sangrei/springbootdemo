stages:
- name: compile
  steps:
  - runScriptConfig:
      image: harbor.wildpig.com:11080/library/maven
      shellScript: mvn clean package
- name: publish
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: springbootdemo:latest
      pushRemote: true
      registry: harbor.wildpig.com
- name: deploy
  steps:
  - applyYamlConfig:
      path: ./deployment.yaml
timeout: 60
